# Re-despliegue SMTP de solicitudes Higo Driver

Fecha: 2026-08-04 UTC

Este marcador fuerza un nuevo despliegue de la rama `main` en Hostinger para sincronizar `public/api/_driver_applications.php` con la versión que usa SMTP autenticado mediante las claves privadas `DRIVER_SMTP_*`.

No contiene credenciales ni secretos.
