# Logs
logs
*.log

# Credenciales SMTP del form de higodriver (subido a mano al VPS)
higodriver/api/_smtp_config.php
public/api/_smtp_config.php
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Variables de entorno con secrets (usar .env.example como plantilla)
.env
.env.local
.env.*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

service-account.json

# Android keystores — NUNCA commitear. La upload key vive como GitHub Secret
# (ANDROID_KEYSTORE_BASE64) y CI la decodifica antes del gradle build. Para
# dev local, guardá el .jks en android/app/ — está ignorado pero presente.
*.jks
*.keystore
*.p12
*.pfx
